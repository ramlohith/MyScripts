#!/usr/bin/python

n,c = map(int,raw_input().split())
x = []
r = 0
x = map(int,raw_input().split())

for j in range(0,n-1):
        if (x[j] - x[j+1]) > r:
                r = x[j] - x[j+1]
if r-c >= 0:
        print r-c
else:
        print "0"

